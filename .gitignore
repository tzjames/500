# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# bot training — checkpoints and logs are large and reproducible. The exported
# model at server/models/bot.json is not ignored: it's what gets deployed.
/ai/runs
__pycache__/
*.pyc

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# card art source bundles — the converted packs live in public/cards/
*.zip

# training scratch: model checkpoints and run logs from the RL pipeline
/ai/__pycache__
/ai/runs
/ai/.venv
